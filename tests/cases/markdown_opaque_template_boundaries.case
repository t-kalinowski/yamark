-- args
format --stdin-file-path input.md --canonical --wrap sentence
-- stdin
Before
{{ render("unterminated }} after [real](  target  ) _word_.

Before
{{ render({nested: 1) }} after [real](  target  ) _word_.

Before
{{ render("multi
line") }} after [real](  target  ) _word_.

Before
{{ render("[x](  url  ) _raw_") }} with `[x](  url  ) _code_` and $[x](  url  )$ after _word_.
-- stdout
Before
{{ render("unterminated }} after [real](  target  ) _word_.

Before
{{ render({nested: 1) }} after [real](  target  ) _word_.

Before
{{ render("multi
line") }} after [real](  target  ) _word_.

Before {{ render("[x](  url  ) _raw_") }} with `[x](  url  ) _code_` and $[x](  url  )$ after *word*.
-- stderr
-- status
0
