-- args
format --stdin-file-path input.md
-- stdin
Short first. This second sentence is deliberately long enough that it must wrap before reaching the configured default column of seventy-two characters. Short last.
-- stdout
Short first.
This second sentence is deliberately long enough that it must wrap
before reaching the configured default column of seventy-two characters.
Short last.
-- stderr
-- status
0
