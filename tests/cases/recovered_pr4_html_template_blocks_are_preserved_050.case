-- args
format --canonical --wrap sentence --stdin-file-path input.md --verify
-- stdin
Before
<span><noembed>const x="<span>"</noembed>keep   _this_ {{ foo }}</span>
after.

Following
prose.
-- stdout
Before
<span><noembed>const x="<span>"</noembed>keep   _this_ {{ foo }}</span>
after.

Following prose.
-- stderr
-- status
0
