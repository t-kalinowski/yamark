-- args
format --canonical --wrap sentence --stdin-file-path input.md --verify
-- stdin
Before
`{{ first }} {{ printf `keep   this` }}`
after.

Following
prose.
-- stdout
Before
`{{ first }} {{ printf `keep   this` }}`
after.

Following prose.
-- stderr
-- status
0
