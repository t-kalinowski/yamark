-- args
format --canonical --wrap sentence --stdin-file-path input.md --verify
-- stdin
Before
`<% result = `printf 'keep   this'` %>`
after.

Following
prose.
-- stdout
Before
`<% result = `printf 'keep   this'` %>`
after.

Following prose.
-- stderr
-- status
0
