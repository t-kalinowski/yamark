-- args
format --stdin-file-path input.md --wrap 24 --canonical
-- stdin
- Before
    `first  
    second [x]( url )` after _outside_ [real](  target  ).

- Before
    $first  
    second \$ [x]( url )$ after _outside_. Next sentence.

# Existing indentation boundaries

- Before
    still prose after _outside_ [real](  target  ).

100. Before
    `first
    second` after _outside_.

# Existing split-link continuation

- Before [label](target
    ) after _outside_ [real](  target  ).
-- stdout
- Before
  `first  
    second [x]( url )`
  after *outside*
  [real](target).

- Before
  $first  
    second \$ [x]( url )$
  after *outside*. Next
  sentence.

# Existing indentation boundaries

- Before
    still prose after _outside_ [real](  target  ).

100. Before
    `first
    second` after _outside_.

# Existing split-link continuation

- Before [label](target)
  after *outside*
  [real](target).
-- stderr
-- status
0
