-- args
format --wrap sentence --stdin-file-path input.md --verify
-- stdin
> Before
> this {{ foo }} after.

Before
<span>{{ foo }}</span> after.

Before
{{ foo }}
after.

Before
{{ foo }} after.

Following
prose.
-- stdout
> Before this {{ foo }} after.

Before
<span>{{ foo }}</span> after.

Before
{{ foo }}
after.

Before {{ foo }} after.

Following prose.
-- stderr
-- status
0
